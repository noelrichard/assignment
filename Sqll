

CREATE DATABASE sales;

USE sales;

CREATE TABLE orders (
  order_id INT,
  customer_id INT,
  order_date DATE,
  order_total DECIMAL(10,2)
);


This will create a database called "sales" and a table called "orders" with the columns "order_id", "customer_id", "order_date", and "order_total".
